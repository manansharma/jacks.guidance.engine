This patch will update `{{{path}}}` to use _https_ instead of _http_

We have verified the content of the script using https to be identical to the content of the script served using the
http protocol.